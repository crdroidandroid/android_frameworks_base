<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical">

    <ImageView
        android:id="@id/dreamliner_gear"
        android:layout_width="@dimen/settings_gear_touch_target_size"
        android:layout_height="@dimen/settings_gear_touch_target_size"
        android:accessibilityLiveRegion="polite"
        android:contentDescription="@string/accessibility_settings_gear"
        android:layout_gravity="end"
        android:layout_marginRight="@dimen/settings_gear_margin"
        android:layout_marginTop="@dimen/status_bar_header_height_keyguard"
        android:scaleType="center"
        android:src="@drawable/ic_settings_gear"
        android:tooltipText="@string/accessibility_settings_gear"
        android:visibility="gone" />

</FrameLayout>
